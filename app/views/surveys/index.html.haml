%h1 Listing surveys

%table.table
  %thead
    %tr
      %th
        survey
      %th
        actions

  %tbody
    - @surveys.each do |survey|
      %tr
        %td
          from:
          = survey.start_at.strftime("%d/%m")
          till:
          = survey.stop_at.strftime("%d/%m")
        %td
          = link_to 'Show', [current_org, survey], class: "btn btn-link"
          |
          = link_to 'Edit', edit_organization_survey_path(current_org, survey), class: "btn btn-link"
          |
          .inline-block{ style: "display: inline-block" }
            = button_to "Destroy", [current_org, survey], method: :delete, class: "btn btn-link"

%br

= link_to 'New Survey', new_organization_survey_path(current_org), class: "btn btn-primary"
