%h1 Listing surveys

%table.table
  %thead
    %tr
      %th
        survey
      %th
        actions

  %tbody
    - @surveys.each do |survey|
      %tr
        %td
          from:
          = survey.start_at.strftime("%d/%m")
          till:
          = survey.stop_at.strftime("%d/%m")
        %td
          = link_to 'Show', survey, class: "btn btn-link"
          |
          = link_to 'Edit', edit_survey_path(survey), class: "btn btn-link"
          |
          .inline-block{ style: "display: inline-block" }
            = button_to "Destroy", survey, method: :delete, class: "btn btn-link"

%br

= link_to 'New Survey', new_survey_path
